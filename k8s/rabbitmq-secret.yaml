# WARNING: This is a TEMPLATE. Do NOT commit real credentials to git.
# For local/dev you may edit the values below. For production, create the
# secret out-of-band and keep it out of version control, e.g.:
#
#   kubectl create secret generic rabbitmq-auth \
#     --from-literal=username="apolo" \
#     --from-literal=password="$(openssl rand -base64 18)"
#
# or manage it with an external secrets manager (Vault, AWS Secrets Manager,
# External Secrets Operator, etc.).
apiVersion: v1
kind: Secret
metadata:
  name: rabbitmq-auth
type: Opaque
stringData:
  username: apolo
  password: CHANGE_ME_STRONG_PASSWORD
