# WARNING: This is a TEMPLATE. Do NOT commit real credentials to git.
# For production, create the secret out-of-band and keep it out of version
# control, e.g.:
#
#   kubectl create secret generic grafana-admin \
#     --from-literal=admin-user="apolo_admin" \
#     --from-literal=admin-password="$(openssl rand -base64 18)"
#
apiVersion: v1
kind: Secret
metadata:
  name: grafana-admin
type: Opaque
stringData:
  admin-user: apolo_admin
  admin-password: CHANGE_ME_STRONG_PASSWORD
